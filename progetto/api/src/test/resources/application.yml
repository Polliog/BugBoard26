spring:
  datasource:
    url: jdbc:h2:mem:bugboard-test;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

bugboard:
  jwt:
    secret: test-secret-key-that-is-at-least-32-characters-long
    expiration: 86400000

server:
  port: 8080
